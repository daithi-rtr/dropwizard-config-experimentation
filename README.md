# dropwizard-config-experimentation
Vanilla dropwizard project, used for experimenting with config system.

To run:

serverTest --brokerhost host --brokerport 12345 --brokeruser user.name --brokerpass derp12345 config.yml
